<style>
  .callnumber {
    white-space: nowrap;
  }
</style>

<h2><%= @data[:summary].list_full_name %></h2>
<%= render "select_summary" %>
<div>
  <p>Items: <b><%= @data[:criteria] %></b></p>
  <!-- TODO: Make the download link honor the current filter -->
  <p><a href="<%= dashboard_details_path(format: 'tsv') %>">Download data (TSV)</a></p>
</div>
<%= render "rows" %>